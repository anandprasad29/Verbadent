import 'package:flutter/material.dart';

/// App color constants based on Figma design
class AppColors {
  // Primary color - Blue used for sidebar and accents
  static const Color primary = Color(0xFF4284F3);
  
  // Background color - White
  static const Color background = Color(0xFFFFFFFF);
  
  // Text colors
  static const Color textPrimary = Color(0xFF0A2D6D); // Dark blue for main titles
  static const Color textSecondary = Color(0xFF000000); // Black for navigation labels
  
  // Secondary/Neutral colors
  static const Color neutral = Color(0xFFD9D9D9); // Light gray for navigation buttons
  
  // Private constructor to prevent instantiation
  AppColors._();
}

